using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

/*
[CustomEditor ( typeof ( QuestionText ) )]
public class CI_QuestionText : Editor {
	public override void OnInspectorGUI ()
	{
		
		DrawDefaultInspector ( );
		QuestionText myTarget = (QuestionText)target;

		if( GUILayout.Button("Add") )
		{
			myTarget.AddOption ( );
		}
		
	}
}

	*/
